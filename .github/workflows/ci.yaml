name: CI
on:
  pull_request:
jobs:
  version:
    name: Go version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
      - name: version
        run: go version
